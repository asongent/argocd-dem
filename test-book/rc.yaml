apiVersion: v1
kind: ReplicationController
metadata:
  name: nginx
spec:
  replicas: 3
  selector:
    app: web-helm-chart
  template:
    metadata:
      name: nginx
      labels:
        app: web-helm-chart
    spec:
      containers:
      - name: nginx
        image: nginx
        ports:
        - containerPort: 80
